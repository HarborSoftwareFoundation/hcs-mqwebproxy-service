version: '3.7'

services:
  hcs-mqwebproxy-api:
    image: ${DOCKER_REGISTRY:-harbor}/hcs/mqwebproxy-api:${PLATFORM:-linux}-${TAG:-latest}
    build:
      context: .
      dockerfile: src/Hcs.MqWebProxy.Api/Dockerfile
